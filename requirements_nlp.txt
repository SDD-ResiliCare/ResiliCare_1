# Optional, EXPERIMENTAL Task 12 (voice intake) dependencies only. Not required to run the
# clinical scorer, the demo server, or the main test suite — everything else in this repo runs
# on the standard library plus nothing else.
#
# NOT VERIFIED in this project's Python 3.14 environment: these pins predate Python 3.14 wheel
# support (torch 2.1.2 in particular). If `pip install -r requirements_nlp.txt` fails here, that
# is expected until this file is re-pinned against a working torch/transformers/spaCy/librosa
# combination for your interpreter, or run under a separate Python 3.10-3.12 environment instead.
librosa==0.10.1
spacy==3.7.2
torch==2.1.2
torchaudio==2.1.2
transformers==4.36.2
soundfile==0.12.1
